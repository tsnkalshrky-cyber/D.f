
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.daftarpro.app',
  appName: 'Daftar Pro',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
